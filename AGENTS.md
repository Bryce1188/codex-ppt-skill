# Repository Notes

This repository contains one Codex skill:

```text
skills/codex-ppt-skill
```

Keep the skill name in `SKILL.md` as `codex-ppt-skill`. Do not commit API keys, generated decks, runtime state, `__pycache__`, or local output folders.

The skill is based on `ningzimu/codex-ppt-skill` and includes local additions for:

- Chinese workflow instructions
- topic and exhibition web research
- project evidence scanning
- textless backgrounds with editable HTML/PPT text layers
